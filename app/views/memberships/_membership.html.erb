<tr>
  <td><%= link_to membership.id, edit_membership_path(membership.id) %></td>
  <td><%= membership.name %></td>
  <td><%= link_to "Delete", membership, method: :delete, data: {confirm: "Are you sure you want to delete this Membership?"} %></td>
</tr>
<tr>
  <td><%= link_to membership.id, edit_membership_path(membership.id) %></td>
  <td><%= membership.name %></td>
  <div class="btn-group" role="group">
  <td><%= link_to "Delete", membership,class: "btn btn-default", method: :delete, data: {confirm: "Are you sure you want to delete this Membership?"} %></td>
  </div>
</tr>

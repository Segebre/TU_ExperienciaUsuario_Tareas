<tr>
  <td><%= link_to comment.id, edit_post_comment_path(comment.post, comment) %></td>
  <td><%= image_tag comment.character.avatar_url, :size => "200x184" %></td>
  <td><%= link_to comment.character.name, comment.character %></td>
  <td><%= comment.message %></td>
  <td><%= nice_date(comment.created_at) %></td>
  <td><%= link_to "Delete", [comment.post, comment], method: :delete, data: {confirm: "Are you sure you want to delete this comment?"} %></td> 
</tr>